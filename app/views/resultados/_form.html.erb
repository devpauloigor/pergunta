<%= form_for(@resultado) do |f| %>
  <% if @resultado.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@resultado.errors.count, "error") %> prohibited this resultado from being saved:</h2>

      <ul>
      <% @resultado.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :provaaluno_id %><br>
    <%= f.text_field :provaaluno_id %>
  </div>
  <div class="field">
    <%= f.label :questao_id %><br>
    <%= f.text_field :questao_id %>
  </div>
  <div class="field">
    <%= f.label :resposta_id %><br>
    <%= f.text_field :resposta_id %>
  </div>
  <div class="field">
    <%= f.label :correta %><br>
    <%= f.number_field :correta %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
